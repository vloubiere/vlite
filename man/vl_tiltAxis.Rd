% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vl_tiltAxis.R
\name{vl_tiltAxis}
\alias{vl_tiltAxis}
\title{Create Tilted Axis Labels}
\usage{
vl_tiltAxis(
  x,
  y = rep(par("usr")[3], length(labels)) - diff(grconvertY(c(0, par("mgp")[2]), "line",
    "user")),
  labels,
  srt = 45,
  offset = 0.25,
  pos = 2,
  xpd = NA,
  cex = par("cex.axis"),
  ...
)
}
\arguments{
\item{x}{Numeric vector specifying x-coordinates for labels.}

\item{y}{Numeric vector specifying y-coordinates for labels. Defaults to
bottom of plot area adjusted by margin parameters.}

\item{labels}{Character vector of labels to be plotted.}

\item{srt}{Numeric angle of rotation in degrees. Default is 45.}

\item{offset}{Numeric offset from the x position in character widths.
Default is 0.25.}

\item{pos}{Integer specifying position relative to point:
1=below, 2=left, 3=above, 4=right. Default is 2.}

\item{xpd}{Logical or NA controlling clipping region. Default is NA
(plotting allowed outside plot region).}

\item{cex}{Numeric character expansion factor. Default is par("cex.axis").}

\item{...}{Additional arguments passed to text().}
}
\value{
Invisibly returns NULL. Called for its side effect of adding tilted
labels to a plot.
}
\description{
Creates tilted labels for x-axis tick marks, improving readability when
dealing with long or numerous labels. This function provides a more
flexible alternative to standard axis labels, particularly useful for
heatmaps, barplots, and other visualizations with potentially overlapping
labels.
}
\details{
The function automatically calculates appropriate y-positions for labels
based on the current plot parameters if not specified. It is particularly
useful for:
* Long category names
* Dense x-axis labels
* Improving readability in complex plots
* Custom axis label positioning
}
\examples{
# Basic usage with long labels
plot(1:5, xaxt = "n", xlab = "")
vl_tiltAxis(1:5, labels = paste("Long Label", 1:5))

}
\seealso{
\code{\link{text}} for underlying text plotting function
\code{\link{par}} for graphical parameters
\code{\link{gImage}} for heatmap plotting function
}
